package unibo.appl1.observer;

import java.util.Observable;
import java.util.Observer;

public class ObserverForPath implements Observer {

    @Override
    public void update(Observable observable, Object o) {

    }
}
