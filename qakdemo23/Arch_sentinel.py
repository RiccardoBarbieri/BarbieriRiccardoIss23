from diagrams import Cluster, Diagram, Edge
from diagrams.custom import Custom
import os
os.environ['PATH'] += os.pathsep + 'C:/Program Files/Graphviz/bin/'

graphattr = {     #https://www.graphviz.org/doc/info/attrs.html
    'fontsize': '22',
}

nodeattr = {   
    'fontsize': '22',
    'bgcolor': 'lightyellow'
}

eventedgeattr = {
    'color': 'red',
    'style': 'dotted'
}
with Diagram('sentinelArch', show=False, outformat='png', graph_attr=graphattr) as diag:
  with Cluster('env'):
     sys = Custom('','./qakicons/system.png')
     with Cluster('ctxsentinel', graph_attr=nodeattr):
          sentinel=Custom('sentinel','./qakicons/symActorSmall.png')
          sender=Custom('sender','./qakicons/symActorSmall.png')
     sys >> Edge(color='red', style='dashed', xlabel='alarm', fontcolor='red') >> sentinel
diag
